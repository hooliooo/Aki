platform :ios, '10.0'

target 'Aki' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  target 'Aki Tests' do
      inherit! :search_paths

      pod 'Quick'
      pod 'Nimble'
  end

end

target 'AkiExample' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for AkiExample

end
